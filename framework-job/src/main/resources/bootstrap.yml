spring:
  application:
    name: framework
  profiles:
    active: local
  cloud:
    consul:
      host: 192.168.0.5
      port: 8500
      config:
        format: YAML
        data-key: data
        profile-separator: '-'
server:
  port: 8080